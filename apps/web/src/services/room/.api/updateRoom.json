{
  "name": "updateRoom",
  "file": "src/services/room/database/room.mutations.ts",
  "sig": "(id: string, orgId: string, data: { name?: string; capacity?: number; locationId?: string; equipment?: string[] }) => Promise<{ id: string; name: string; capacity: number | null; equipment: string[]; location: { id: string; name: string; } | null; locationId: string | null; }>",
  "composes": [],
  "rules": [],
  "why_ref": ""
}

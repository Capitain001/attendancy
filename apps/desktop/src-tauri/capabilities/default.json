{
  "$schema": "../node_modules/@tauri-apps/cli/schema/capabilities.schema.json",
  "identifier": "default",
  "description": "Default capability — accès réseau pour appels API Next.js",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}

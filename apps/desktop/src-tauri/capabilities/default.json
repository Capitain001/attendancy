{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Permissions par défaut de l'app Attendancy",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}

// Types partagés entre apps/web, apps/desktop, apps/mobile
// Générés depuis apps/web/src/services/*/types.ts — ne pas éditer manuellement.
// Ajouter ici uniquement les types cross-platform (DTO lecture seule).

export type { ScheduleSlot, ScheduleDto, DayScheduleDto } from './schedule'
export type { SessionDto, ActiveSessionDto } from './session'
export type { PlanningSlotDto } from './planning'

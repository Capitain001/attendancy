// "use client";

// import { useOrganizationContext } from "@/components/providers/OrganizationProvider";

// export const useOnlineStatus = (userId: string) => {
//   const { onlineUsers } = useOrganizationContext();
//   return Boolean(onlineUsers[userId]);
// };
